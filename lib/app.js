// Generated by CoffeeScript 1.6.3

/*
  * Module dependencies.
 */

(function() {
  var RecipeSearch, app, express, http, mongoURI, mongoose, path, request, routes, yummlyAppId, yummlyAppKey;

  console.log("fire app.js");

  express = require('express');

  routes = require('./routes');

  http = require('http');

  path = require('path');

  mongoose = require('mongoose');

  request = require('request');

  app = express();

  app.set('port', process.env.PORT || 3000);

  app.set('views', __dirname + '/../views');

  app.set('view engine', 'jade');

  app.use(express.favicon());

  app.use(express.logger('dev'));

  app.use(express.bodyParser());

  app.use(express.methodOverride());

  app.use(app.router);

  app.use(express["static"](path.join(__dirname, '/../public')));

  if ('development' === app.get('env')) {
    app.use(express.errorHandler());
  }

  yummlyAppId = '48b32423';

  yummlyAppKey = "f801fe2eacf40c98299940e2824de106";

  mongoURI = process.env.MONGOHQ_URL || 'mongodb://localhost/veganizzmapp';

  mongoose.connect(mongoURI);

  RecipeSearch = mongoose.model('RecipeSearch', {
    search: String,
    cuisine: String,
    courses: String,
    allergies: String,
    diets: String
  });

  app.get('/', routes.index);

  app.post('/submitrecipe', function(req, res) {
    var recipeSearch, submittedInfo;
    submittedInfo = req.body;
    console.log("submitedInfo", submittedInfo);
    recipeSearch = new RecipeSearch(submittedInfo);
    recipeSearch.save(function(err, data) {
      console.log("sent to database:", data);
    });
    console.log(submittedInfo.search);
    request("http://api.yummly.com/v1/api/recipes?_app_id=" + yummlyAppId + "&_app_key=" + yummlyAppKey + "&q=" + submittedInfo.search, function(error, response, body) {
      var recipeObj, yummlyObj;
      recipeObj = {};
      yummlyObj = JSON.parse(body);
      recipeObj.totalMatchCount = yummlyObj['totalMatchCount'];
      recipeObj.criteria = yummlyObj['criteria'];
      recipeObj.matches = yummlyObj['matches'];
      res.send(recipeObj);
      res.send(yummlyObj);
    });
  });

  http.createServer(app).listen(app.get('port'), function() {
    console.log('Express server listening on port ' + app.get('port'));
  });

}).call(this);
